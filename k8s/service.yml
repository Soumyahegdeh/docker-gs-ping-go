apiVersion: v1
kind: Service
metadata:
  name: mygo-service
  labels:
    app: mygo-app
spec:
  selector:
    app: go-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007
  type: NodePort